{
    "name": "smarthub-workspace",
    "version": "1.0.0",
    "description": "SmartHub Travel - Workspace for Client and Server",
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "start": "concurrently \"npm run start --prefix server\" \"npm run dev --prefix client\"",
        "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
        "install:all": "npm install",
        "build": "cross-env NODE_OPTIONS=--max-old-space-size=4096 npm run build --workspace=client",
        "server": "npm run dev --prefix server",
        "client": "npm run dev --prefix client"
    },
    "devDependencies": {
        "concurrently": "^9.1.0",
        "cross-env": "^7.0.3"
    }
}